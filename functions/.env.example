# Copy to functions/.env for local emulator, or set in Firebase console / deploy secrets.
BUNNY_STREAM_PULL_ZONE_HOSTNAME=vz-xxxxxxxx.b-cdn.net
# Pull Zone → Security → Token Authentication Key (not the Stream library API key)
BUNNY_CDN_TOKEN_AUTH_KEY=
