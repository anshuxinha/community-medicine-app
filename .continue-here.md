# Continue Here - 2026-05-14

## Current Status
**Phase:** 03-apple-signin-fix (Closing) -> iOS Subscription Investigation (Active)
**Context:** Apple Sign-In is fixed. We are currently debugging why subscription plans are not showing up on iOS devices.

## Completed Work
- **Apple Sign-In Fix:** Registered native iOS app in Firebase, aligned Bundle IDs (`com.communitymed.app`), and updated `app.json` with `GoogleService-Info.plist`.
- **Test Hardening:** Fixed a Jest environment mismatch in `PaywallScreen.analytics.test.js` by mocking `@react-native-google-signin/google-signin`.
- **Infrastructure:** Started an Expo development tunnel (`exp://1kki-d8-anshuxinha-8081.exp.direct`) for on-device debugging on iPad.

## Remaining Work
- **RevenueCat iOS Config:** The app reports "no App Store products registered" and "Configuration is not valid" for iOS. Needs verification in the RevenueCat Dashboard.
- **On-Device Debugging:** User is currently scanning the tunnel to see if logs reveal more detail or if dashboard changes fix the issue.
- **Commit & Documentation:** Phase 3 changes (including `3-CONTEXT.md` and `GoogleService-Info.plist`) are currently unstaged/untracked. Need to commit once the subscription issue is clarified or as a separate atomic step.

## Decisions & Blockers
- **Decision:** Locked primary Bundle ID to `com.communitymed.app`.
- **Blocker:** Missing product/offering configuration in RevenueCat for the iOS platform (as per error logs).

## Resume Instructions
1. Run `npx expo start --tunnel` if the current process has terminated.
2. Check `src/context/AppContext.js` and `src/screens/PaywallScreen.js` for dynamic RevenueCat key selection logic.
3. Review terminal logs for `[RevenueCat]` tags while the user interacts with the Paywall on the iPad.
