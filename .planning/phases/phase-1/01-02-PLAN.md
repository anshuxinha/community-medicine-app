---
phase: 01-coupon-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [
  "package.json",
  "jest.config.js",
  "src/services/couponService.js",
  "src/screens/PaywallScreen.js",
  "src/services/__tests__/couponService.test.js"
]
autonomous: true
requirements: [COUPON-01, COUPON-02, COUPON-03]
user_setup:
  - service: RevenueCat
    why: "Dynamic price targeting based on coupon attributes"
    dashboard_config:
      - task: "Create Targeting Rule"
        location: "RevenueCat Dashboard -> Targeting"
        details: "Define a rule mapping the 'coupon_code' subscriber attribute to an Offering with discounted packages. This ensures the Store Modal matches the app UI."

must_haves:
  truths:
    - "Test runner is configured and executes via 'npm test'."
    - "Coupon usage is enforced by atomic Firestore transactions."
    - "Applying a coupon updates RevenueCat subscriber attributes and re-triggers offering fetch."
    - "Price parsing handles international formats (e.g., '9,99 €') correctly in UI previews."
  artifacts:
    - path: "jest.config.js"
      provides: "Jest configuration for Expo environment"
    - path: "src/services/couponService.js"
      provides: "Transactional usage increments and robust currency parsing"
    - path: "src/services/__tests__/couponService.test.js"
      provides: "Mocks for Firestore transactions and international currency test cases"
  key_links:
    - from: "src/screens/PaywallScreen.js"
      to: "react-native-purchases"
      via: "setAttributes and getOfferings"
    - from: "src/services/__tests__/couponService.test.js"
      to: "src/services/couponService.js"
      via: "Unit testing of exported logic"
---

<objective>
Hardened implementation of coupon logic addressing targeting mismatches, testing gaps, and parsing fragility. This plan establishes a robust testing baseline and synchronizes app-side discounts with RevenueCat's store-side targeting, replacing static UI disclaimers with dynamic store behavior.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@1-CONTEXT.md
@.planning/phases/phase-1/01-RESEARCH.md
@.planning/phases/phase-1/VERIFICATION.md
@src/services/couponService.js
@src/screens/PaywallScreen.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testing Infrastructure & Config</name>
  <files>package.json, jest.config.js</files>
  <action>
    1. Install dev dependencies: `jest`, `babel-jest`, `jest-expo`, and `@testing-library/react-native`.
    2. Add `"test": "jest"` to `package.json` scripts.
    3. Create `jest.config.js` with:
       ```javascript
       module.exports = {
         preset: 'jest-expo',
         transformIgnorePatterns: [
           'node_modules/(?!((jest-)?react-native|@react-native(-community)?)|expo(nent)?|@expo(nent)?/.*|@expo-google-fonts/.*|react-navigation|@react-navigation/.*|@unimodules/.*|unimodules|sentry-expo|native-base|react-native-svg)'
         ]
       };
       ```
    4. Ensure `.babelrc` or `babel.config.js` is present and compatible with `babel-jest`.
  </action>
  <verify>
    <automated>npm test -- --version && test -f jest.config.js</automated>
  </verify>
  <done>Testing framework is installed and configured for the React Native/Expo environment.</done>
</task>

<task type="auto">
  <name>Task 2: Transactional Logic & RC Targeting</name>
  <files>src/services/couponService.js, src/screens/PaywallScreen.js</files>
  <action>
    1. **Firestore Transactions**: Refactor `incrementCouponUsage` in `couponService.js` to use `runTransaction`. Read doc -> check `usageCount < usageLimit` -> increment. Throw error on limit reach.
    2. **RC Targeting**: In `PaywallScreen.js`, replace the static UI disclaimer ("price may differ") with dynamic targeting logic:
       - Success handler: `await Purchases.setAttributes({ "coupon_code": coupon.code })`.
       - Re-fetch offerings: `await Purchases.getOfferings()`.
       - Update screen state to use the packages from the new offerings, ensuring UI price matches RC targeting rules.
    3. **Network Check**: Guard coupon application with `NetInfo.fetch()` to ensure live validation.
  </action>
  <verify>
    <automated>grep -q "runTransaction" src/services/couponService.js && grep -q "setAttributes" src/screens/PaywallScreen.js</automated>
  </verify>
  <done>Backend logic is atomic, and the frontend synchronizes coupon state with RevenueCat for dynamic pricing, removing the need for UI disclaimers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Robust Parsing & Transaction Tests</name>
  <files>src/services/couponService.js, src/services/__tests__/couponService.test.js</files>
  <behavior>
    - applyDiscount("9,99 €", {type: 'percentage', value: 10}) -> "8,99 €"
    - applyDiscount("₹ 1.000,00", {type: 'flat', value: 500}) -> "₹ 500,00"
    - applyDiscount("$10.00", {type: 'percentage', value: 20}) -> "$8.00"
    - incrementCouponUsage should call runTransaction and fail if mock count >= limit.
  </behavior>
  <action>
    1. **Robust Parsing**: Refactor `applyDiscount` to handle comma decimals and currency symbol positioning using a robust regex/parseFloat strategy.
    2. **Transaction Mock Test**: In `src/services/__tests__/couponService.test.js`, mock the Firestore `runTransaction` to verify the "read-before-write" increment logic and error handling when the limit is reached.
    3. **International Currency Tests**: Add test cases for the specific formats listed in behavior to ensure parsing resiliency.
  </action>
  <verify>
    <automated>npm test src/services/__tests__/couponService.test.js</automated>
  </verify>
  <done>Calculation logic handles global pricing formats, and transaction safety is verified via mocked unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → Firestore | Transactions ensure the client cannot unilaterally bypass usage limits. |
| Client → RevenueCat | setAttributes allows RC to serve as the source of truth for discounted pricing. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | couponService.js | mitigate | Use runTransaction to prevent race conditions on usageLimit. |
| T-01-04 | Repudiation | PaywallScreen.js | mitigate | Synchronize coupon_code attribute with RevenueCat to ensure auditability of discounted purchases. |
| T-01-05 | Information Disclosure | PaywallScreen.js | mitigate | Use NetInfo to prevent validating coupons against stale or offline data. |
</threat_model>

<verification>
1. Run `npm test` to ensure infrastructure and all test cases pass.
2. Verify that `setAttributes` is called upon coupon entry.
3. Manually test with a comma-separated currency value (e.g., log the output of applyDiscount).
</verification>

<success_criteria>
- Jest is configured and functional.
- Coupons are applied via RevenueCat targeting attributes.
- Price parsing is resilient to international formats.
- Firestore usage increments are atomic and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-1/01-02-SUMMARY.md`
</output>
