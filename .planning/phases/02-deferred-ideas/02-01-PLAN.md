---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["src/services/topicIllustrations.js", "src/services/__tests__/topicIllustrations.test.js", "scripts/validate-illustrations.js"]
autonomous: true
requirements: ["D-01", "D-02", "D-03", "D-04"]

must_haves:
  truths:
    - "Gem images use 'gems/' path in Storage URLs"
    - "Illustrations fetch falls back to contentKey query if docId fails"
    - "Validation script identifies illustrations missing URLs"
  artifacts:
    - path: "src/services/topicIllustrations.js"
      provides: "Resilient asset fetching logic"
      min_lines: 100
    - path: "src/services/__tests__/topicIllustrations.test.js"
      provides: "Unit tests for fetching logic"
      min_lines: 50
    - path: "scripts/validate-illustrations.js"
      provides: "Data quality validation"
      min_lines: 30
  key_links:
    - from: "src/services/topicIllustrations.js"
      to: "Firebase Storage"
      via: "normalizeIllustration URL construction"
      pattern: "storage\\.googleapis\\.com"
    - from: "src/services/topicIllustrations.js"
      to: "Firestore topicIllustrations"
      via: "query fallback"
      pattern: "query.*collection.*where.*contentKey"
---

<objective>
Harden Gem image loading reliability and ensure data quality in Firestore metadata.
Purpose: Decouple illustration fetching from brittle title-based IDs and fix incorrect storage paths for Gems.
Output: Resilient topicIllustrations service, validation script, and comprehensive unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deferred-ideas/2-CONTEXT.md
@.planning/phases/02-deferred-ideas/02-RESEARCH.md
@src/services/topicIllustrations.js

<interfaces>
From src/services/topicIllustrations.js:
```javascript
export const getTopicIllustrations = async ({
  section,
  topicId,
  contentKey,
}) => { ... }

const normalizeIllustration = (image = {}) => { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update topicIllustrations.js for multi-path and query fallback</name>
  <files>src/services/topicIllustrations.js</files>
  <action>
    Modify `normalizeIllustration` to accept an optional `basePath` (default "reading-illustrations").
    Update `getTopicIllustrations` to:
    1. Determine `basePath` based on `contentKey`. If `contentKey` starts with "gems:", use "gems" as `basePath`.
    2. Pass `basePath` to `normalizeIllustration`.
    3. Implement a fallback query in `getTopicIllustrations` using `where("contentKey", "==", resolvedContentKey)` if the initial `getDoc(docId)` returns no data or fails (per D-02, D-03).
  </action>
  <verify>
    <automated>grep -E "basePath|query|where" src/services/topicIllustrations.js</automated>
  </verify>
  <done>Service handles 'gems:' prefix and falls back to query-based fetching.</done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for topicIllustrations service</name>
  <files>src/services/__tests__/topicIllustrations.test.js</files>
  <action>
    Create comprehensive tests for `getTopicIllustrations`:
    - Test that "gems:..." contentKey results in URLs containing "/gems/".
    - Test that standard keys result in "/reading-illustrations/".
    - Mock Firestore to simulate a `getDoc` failure/missing doc and verify the query fallback logic successfully retrieves the mock data.
  </action>
  <verify>
    <automated>npm test src/services/__tests__/topicIllustrations.test.js</automated>
  </verify>
  <done>All 3 core test cases (pathing, fallback, normalization) pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create Firestore metadata validation script</name>
  <files>scripts/validate-illustrations.js</files>
  <action>
    Create a Node.js script (using firebase-admin or client SDK) to scan the `topicIllustrations` collection.
    The script should:
    1. Fetch all documents.
    2. Check if `images` array exists and contains valid `url` or `fileName` fields.
    3. Report any documents that are missing metadata (per D-04).
  </action>
  <verify>
    <automated>node scripts/validate-illustrations.js --dry-run</automated>
  </verify>
  <done>Script successfully identifies documents with incomplete metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firestore -> Client | Metadata from Firestore is trusted for structure but verified for URL presence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | topicIllustrations.js | mitigate | Sanitize `fileName` in storage URL construction to prevent path traversal (per Research). |
</threat_model>

<verification>
Run `npm test src/services/__tests__/topicIllustrations.test.js` to ensure logical correctness.
Run `node scripts/validate-illustrations.js` to verify data integrity in the dev environment.
</verification>

<success_criteria>
- Gem images constructed with correct 'gems/' path.
- Service resilient to missing/brittle docIds.
- Validation script providing actionable data quality reports.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deferred-ideas/02-01-SUMMARY.md`
</output>
