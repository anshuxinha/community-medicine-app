---
phase: "02"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified: ["src/screens/PaywallScreen.js", "src/screens/__tests__/PaywallScreen.analytics.test.js"]
autonomous: true
requirements: ["D-05", "D-06"]

must_haves:
  truths:
    - "Coupon apply attempt logged to Firebase Analytics"
    - "Coupon apply success logged with plan details"
    - "Coupon apply failure logged with error reason"
    - "appliedCoupon field synced to user document in Firestore"
  artifacts:
    - path: "src/screens/PaywallScreen.js"
      provides: "Analytics triggers and sync logic"
      min_lines: 400
    - path: "src/screens/__tests__/PaywallScreen.analytics.test.js"
      provides: "Test verification for analytics calls"
      min_lines: 50
  key_links:
    - from: "src/screens/PaywallScreen.js"
      to: "Firebase Analytics"
      via: "logEvent from config/firebase"
      pattern: "logEvent.*analytics"
    - from: "src/screens/PaywallScreen.js"
      to: "Firestore users"
      via: "upgradeToPremium context call"
      pattern: "upgradeToPremium.*appliedCoupon"
---

<objective>
Implement Firebase Analytics tracking for the coupon funnel and verify Firestore synchronization.
Purpose: Provide visibility into coupon performance and ensure billing support has access to applied coupon data.
Output: Instrumented PaywallScreen and automated tests verifying analytics dispatch.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deferred-ideas/2-CONTEXT.md
@.planning/phases/02-deferred-ideas/02-RESEARCH.md
@src/screens/PaywallScreen.js

<interfaces>
From src/screens/PaywallScreen.js:
```javascript
const PaywallScreen = ({ navigation }) => {
  const handleApplyCoupon = async () => { ... }
  const handlePurchase = async () => { ... }
}
```

From src/config/firebase.js (expected):
```javascript
export const analytics = getAnalytics(app);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Analytics logging to PaywallScreen.js</name>
  <files>src/screens/PaywallScreen.js</files>
  <action>
    Import `logEvent` from `firebase/analytics` and `analytics` from `../config/firebase`.
    Implement `logCouponEvent` helper in `PaywallScreen.js` (per Research Pattern).
    Add triggers to `handleApplyCoupon`:
    1. Log `coupon_apply_attempt` (params: code, selectedPlan).
    2. Log `coupon_apply_success` (params: code, plan, discount_type).
    3. Log `coupon_apply_failure` (params: code, reason).
    Add trigger to `handlePurchase`:
    1. Log `purchase_success` (params: plan, coupon: appliedCoupon?.code).
    Verify that `upgradeToPremium` in `handlePurchase` correctly passes `appliedCoupon` to sync with Firestore user document (per D-05, D-06).
  </action>
  <verify>
    <automated>grep -E "logEvent|coupon_apply_attempt|coupon_apply_success" src/screens/PaywallScreen.js</automated>
  </verify>
  <done>Analytics events are dispatched at key points in the coupon workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Create analytics integration tests for PaywallScreen</name>
  <files>src/screens/__tests__/PaywallScreen.analytics.test.js</files>
  <behavior>
    - Mock firebase/analytics logEvent.
    - Simulate 'Apply' button press with valid coupon -> verify logEvent called with 'coupon_apply_success'.
    - Simulate 'Apply' button press with invalid coupon -> verify logEvent called with 'coupon_apply_failure'.
  </behavior>
  <action>
    Create a test file using `@testing-library/react-native`.
    Mock the `analytics` object and `logEvent` function.
    Verify that interaction with the UI triggers the expected analytics calls.
  </action>
  <verify>
    <automated>npm test src/screens/__tests__/PaywallScreen.analytics.test.js</automated>
  </verify>
  <done>Tests confirm analytics logging occurs correctly based on UI actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Analytics | Events sent to Firebase Analytics are considered non-sensitive/non-PII tracking. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Denial of Service | PaywallScreen.js | mitigate | Ensure analytics calls are not in render loops or infinite retry cycles (per Research). |
</threat_model>

<verification>
Run `npm test src/screens/__tests__/PaywallScreen.analytics.test.js`.
Manual Verification: Use Firebase DebugView while applying coupons in the app.
</verification>

<success_criteria>
- Coupon funnel metrics visible in Firebase Analytics.
- No regression in coupon application or purchase flow.
- Firestore user documents reliably contain `appliedCoupon` metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/02-deferred-ideas/02-02-SUMMARY.md`
</output>
